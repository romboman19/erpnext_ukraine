# Gate 0A evidence — 2026-07-13

## Result

`PASS`: базове середовище, scheduler/worker і backup/restore rehearsal
підтверджені в ізольованому `frappe-test`.

## Isolation

- test compose project: `frappe-test`;
- test site: `postest.local`;
- test endpoint: port `8081`;
- production compose project `frappe` не змінювався;
- test backup створено перед install-app.

## Versions

| Component | Version |
|---|---:|
| Frappe | 16.25.0 |
| ERPNext | 16.26.2 |
| erpnext_ua | 0.2.0 |
| ukrainian_integrations | 0.1.0 |
| print_designer | 1.6.5 |
| erpnext_consignment_and_commission | 0.1.0 (`agent/stage-0-scaffold`, `0c06a22`) |
| Python in test image | 3.14 |
| MariaDB | 11.8 |

## Deployment evidence

- feature checkout mounted read-only into test configurator/backend/worker;
- backend and worker receive the checkout through explicit `PYTHONPATH`;
- `install-app erpnext_consignment_and_commission` completed;
- `bench --site postest.local migrate` completed with exit code 0;
- site reports the app in `frappe.get_installed_apps()`;
- HTTP smoke test returned `200`;
- readiness status: `ready_for_transactional_spikes`;
- readiness blocking checks: `[]`.

## Backup

Backup with database, public files, private files and site config completed at
`2026-07-13 14:18 UTC`. Restore has not yet been rehearsed, therefore the
backup/restore criterion remains open.

## Completion update — 2026-07-13 19:05 UTC

The application test overlay now defines a scheduler only for the isolated
`frappe-test` project. `frappe-test-scheduler-1` was started without restarting
or changing production. `bench doctor` reported one online worker and no
queued jobs.

A fresh backup with files completed successfully:

- database: `20260713_190545-postest_local-database.sql.gz` (about 1.3 MiB);
- public files: `20260713_190545-postest_local-files.tar`;
- private files: `20260713_190545-postest_local-private-files.tar`;
- site config backup was created separately by `bench backup`.

The backup was restored into a new site, `postest-restore.local`, rather than
overwriting `postest.local`. The restored copy then completed `bench migrate`.
Its scheduler is explicitly paused so only the original test site is scheduled.

Both sites reported the same six installed apps and readiness status
`ready_for_transactional_spikes` with no blocking checks. Their read-only row
fingerprints matched exactly:

| DocType | Original | Restored |
|---|---:|---:|
| Stock Ledger Entry | 140 | 140 |
| GL Entry | 328 | 328 |
| Custom Field | 115 | 115 |
| TP Spike Lot | 31 | 31 |
| Purchase Invoice | 6 | 6 |

The restored copy is retained as a paused rehearsal artifact. Production was
not queried, restarted, migrated or written during this procedure.

## Remaining non-gate work

Full Frappe/ERPNext integration CI still needs a reproducible test-site job.
This belongs to Stage 1 and no longer blocks the Stage 0 exit criterion.
