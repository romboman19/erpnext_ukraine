# ADR-004: Token security

Accepted. HMAC is lookup authority; encrypted Password storage is optional for delivery. APIs mask token. Full token is available only at issue or one-time protected Print Grant; `print_token_once` reprints via replacement.
