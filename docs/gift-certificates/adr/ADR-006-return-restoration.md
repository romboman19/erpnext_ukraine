# ADR-006: Return restoration

Accepted. Restore is derived from original item-level allocation and funding snapshot with last-residual rounding. Replaced/expired targets resolve through lineage; unsafe return cancellation is blocked.
