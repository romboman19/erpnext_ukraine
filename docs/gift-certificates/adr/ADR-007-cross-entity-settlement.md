# ADR-007: Cross-entity settlement

Accepted. Issuer identity is immutable; redeemer comes from final item/FOP. Different entities create signed settlement subledger rows. Return/cancel creates linked inverse rows; no automatic cash transfer.
